#include "main.h"

/**
 ** _abs - function to check
 *
 * @n: cheks number
 *
 * Return: returns 1 if 'c' is lowercase, 0 (Success)
 *
 */

int _abs(int)
{
	int n;

	if (n > 0)
		n = n * (-1);
	return (n);
}
