#include "main.h"

/**
 * print_rev - imprime en reversa
 * @s: string
 */

void print_rev(char *s)
{
	char *st = s;


	while (*str != '\0')
	{
		st++
	}
	st--;
	while (str >= s)
	{
		putchar(*st);
		st--;
	}
	putchar('\n');
}
